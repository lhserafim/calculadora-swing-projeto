1. JFrame
    1.1. JFrame é a classe java que permite a criação de telas
    1.2. É possível setar alguns atributos básicos e ao instanciar uma classe que extends JFrame já ter uma
         tela desenhada.
         Ex.:
            public class Calculadora extends JFrame {
                public Calculadora() {
                    setVisible(true);
                    setDefaultCloseOperation(EXIT_ON_CLOSE); // poderia ser DISPOSE_ON_CLOSE
                    setSize(232,322);
                    setLocationRelativeTo(null);
                }